#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char * shortestCompletingWord(char * licensePlate, char ** words, int wordsSize);

int main(){
    char lincensePlate[7] = "1s3 456";
    char words[][10] = {"step","steps","stripe","stepple"};

}

char * shortestCompletingWord(char * licensePlate, char ** words, int wordsSize){
    int i, j;
    char shortword[], word[];
    for(i=0;i<words[];i++;){
        wordsSize = strlen(words[]);
            for(j=0;j<wordsSize;j++){

            j++;
            }
        if(shortword[]<word[]){
            shortword[]=word[];
            }
        i++;
    }
    return shortword;
}
